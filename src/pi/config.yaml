# Device-specific overrides (not shipped via OTA).
# All other settings come from defaults.yaml.

recording:
  output_dir: /home/pi/clips

upload:
  bucket_name: ""          # Set by setup-pi.sh
  region: eu-west-1

iot:
  endpoint: ""             # Set by setup-pi.sh
  thing_name: ""           # Set by setup-pi.sh
